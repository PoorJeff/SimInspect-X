# P2 — Robot, Plant, Assets
Build differential-drive robot + LiDAR + IMU + RGB camera.
Create plant with 5–8 gauge assets.
Create semantic asset YAML and RViz markers for candidate viewpoints.
Exit:
robot moves, sensors work, assets/viewpoints visible.
