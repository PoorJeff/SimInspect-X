# Data policy
Large generated data should not be committed unless intentionally sampled.
Every dataset must record generator/config/seed/split.
Keep ground-truth gauge labels unavailable to runtime perception nodes.
