# Blueprint Version

Version: **2.1 — OpenCode Runtime Integration**

Changes from v2:
- added project-local `TaskBuilder` primary agent;
- added read-only `Project-Auditor` subagent;
- added project-local commands for task build / work / verify / audit;
- documented `.agent/` vs `.opencode/` responsibilities;
- preserved existing global Phase-Orchestrator / Session-Bootstrap / Session-Handoff architecture;
- no project research/robotics scope change.
